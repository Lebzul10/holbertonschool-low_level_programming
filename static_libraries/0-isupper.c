#include "main.h"
/**
 * _isupper - Something usefull
 * @c: Something more usefull
 *
 * Return: Something much more usefull
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
