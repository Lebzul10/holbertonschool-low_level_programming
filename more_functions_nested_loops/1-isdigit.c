#include "main.h"
/**
 * _isdigit - Something usefull
 * @c: Something more usefull
 *
 * Return: Something much more usefull
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
