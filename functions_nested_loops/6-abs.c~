#include "main.h"
/**
 * _abs - Something usefull
 * @n: Something more usefull
 *
 * Return: Something much more usefull
 */
int _abs(int)
{
  if (n < 0)
	{
	  return (-n);
	}
  else
	{
	  return (n);
	}
}
